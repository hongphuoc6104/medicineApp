## Lý do archive

User đã đổi ưu tiên active sang việc `việt hóa toàn bộ phần hiển thị cho người dùng trong app`.

Batch `Prescription Plan Group Redesign` chưa được nghiệm thu hoàn tất, nên được park lại ở đây để giữ lịch sử và tránh tiếp tục bị đọc như plan active tại root.
