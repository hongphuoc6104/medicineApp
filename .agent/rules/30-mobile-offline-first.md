# Mobile Offline-First Rules

- local notification is the baseline reminder channel
- offline user actions must not be dropped silently
- queued medication logs must flush when connectivity returns or on refresh
- UI must always communicate whether a state is synced, pending sync, or failed
- one-tap actions should remain available for today's doses
