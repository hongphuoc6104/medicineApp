# Artifact Manifests

Store only metadata here: artifact ID, source, license, PHI classification, schema
version, byte size, SHA-256, split ID, model revision, and external storage URI.

Raw prescriptions and model weights must remain outside Git.
