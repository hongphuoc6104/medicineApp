# Shared — Module Dùng Chung

| File | Mô tả |
|------|-------|
| `zero_pima_loader.py` | Load + cache checkpoint `zero_pima_best.pth`. Dùng cho Phase B (FRCNN + GCN match) — Phase A không còn sử dụng |
